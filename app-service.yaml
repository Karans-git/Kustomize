apiVersion: v1
kind: Service
metadata:
  name: jenkins-clusterip
  labels:
    app: jenkins
spec:
  type: ClusterIP
  selector:
    app: jenkins
  ports:
    - port: 8080
      targetPort: 8080
      name: web